package dp_iterator_zoo_animals;

//  Home built iterator interface, no longer used after switching
// to java.util.Interface.  Kept as artifact.

public interface Iterator {
	public boolean hasNext();
	public Animal next();
}
